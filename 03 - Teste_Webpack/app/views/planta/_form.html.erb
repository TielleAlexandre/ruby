<%= form_with(model: plantum) do |form| %>
  <% if plantum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(plantum.errors.count, "error") %> prohibited this plantum from being saved:</h2>

      <ul>
        <% plantum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :cor %>
    <%= form.text_field :cor %>
  </div>

  <div class="field">
    <%= form.label :valor %>
    <%= form.text_field :valor %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
